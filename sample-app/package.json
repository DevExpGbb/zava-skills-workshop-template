{
  "name": "sample-app",
  "version": "0.1.0",
  "description": "Workshop sample app — a deliberately under-tested calculator. Trainees build a skill to fill in the gaps.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/"
  }
}
